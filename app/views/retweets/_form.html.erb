<%= form_for(@retweet) do |f| %>
  <% if @retweet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@retweet.errors.count, "error") %> prohibited this retweet from being saved:</h2>

      <ul>
      <% @retweet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :tweet_id %><br>
    <%= f.number_field :tweet_id %>
  </div>
  <div class="field">
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
